{{>header}}
{{>nav}}
<section class="container my-5">
    <h3 class="text-primary">{{title}}</h3>
<form method="post">
    <div class="mb-3">
        <input name="withdraw" placeholder="withdraw from your current balance" class="form-control">
    </div>

    <div class="mb-3">
        <input type="submit" value="withdraw from balance" class="btn btn-primary">
    </div>
</form>

</section>

{{>footer}}